// Show an alert with the message "Welcome to our website!".
alert("Welcome to our website!");

// Declare a variable called `name` and assign it the value "Lua".
var name = "Lua";

// Create a variable called `age` and assign it the value 25.
var age = 25;

// Define a variable `numOfSales` and assign it the value 50.
var numOfSales = 50;

// Define a variable `availableBalance` and assign it the value 1000.
var availableBalance = 1000;

// Display an alert with the text "Error! Please fill in all fields"
alert("Error! Please fill in all fields");

// Declare a variable called `errorMessage` and assign it the value "Error! Fill in all fields". Now display an alert with the value of the ErrorMessage variable.
var errorMessage = "Error! Fill in all fields";
alert(errorMessage);

// Use a prompt to ask for the user's name and store it in the `userName` variable.
var userName = prompt("What is your name?");
console.log(userName);

// Ask the user to enter their age using a prompt and store it in the `userAge` variable.
var userAge = prompt("What is your age?");
console.log(age);

// Now, if the age is greater than or equal to 18, display an alert with the message "You can get your license!".
var customerAge = prompt("What is your age?");
if (customerAge >= 18) {
  alert("You can get your license!");
}